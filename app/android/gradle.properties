org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Use the Play-Services (unbundled) ML Kit barcode model instead of the in-app
# bundled one: the bundled .tflite model fails to initialize under this
# toolchain (camera "genericError" / a null ML Kit component on start).
dev.steenbakker.mobile_scanner.useUnbundled=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
